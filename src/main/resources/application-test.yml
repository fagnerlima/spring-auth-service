spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth-test
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        show_sql: false
  cache:
    type: NONE
